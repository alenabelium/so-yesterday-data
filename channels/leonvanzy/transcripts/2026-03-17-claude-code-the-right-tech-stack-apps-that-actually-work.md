---
title: "Claude Code + The Right Tech Stack = Apps That Actually Work"
video_id: e6fqES1ygAQ
date: 2026-03-17
url: https://www.youtube.com/watch?v=e6fqES1ygAQ
channel: Leon van Zyl
duration: "25:23"
source: auto-generated
---

# Claude Code + The Right Tech Stack = Apps That Actually Work

**[00:00:00]** Tell me if this ever happened to you. You vibrate an application using your favorite agent coding tool. Maybe you even use planning mode or some kind of spec framework to map everything out and we then get our app to a point where everything seems to be working fine. But when we then test a few edge cases or when we try to add new features or even deploy this app to production, everything just falls apart. In my experience working with many vibe coders and vibe coded projects, the issues almost certainly come down to not understanding the underlying tech stack. And I get it. For a lot of vibe coders, understanding the tech stack can be scary. It sounds complicated and all of these little buzzwords make absolutely no sense. But in this video, I'm going to demystify what a tech stack is, what all these little components are, and what they all do. This really is a beginner video. So, if you're a VIP coder or a junior developer, then this is the perfect video for you. This video is going to be broken up into three parts. First, we're going to learn how the tech stack works by expanding on this application. Then, secondly, I'm going to show you a tip to get your coding agent to select the correct text stack from the get- go. And finally, I'll give you a boilerplate template that you can use right now to ensure that you have a text stack that is scalable, easy to deploy, and everything just works. It is worth mentioning that this is not the only tech stack that's out there. There's actually tons of different frameworks and libraries and ways to approach the same problem. If you're new to all of this, my suggestion to you is to stick with one tech stack.

**[00:01:32]** It's really easy to get confused because you see what other people are doing. Then you also want to learn their tech stack. It just feels completely overwhelming when you do that. Choose one system that works for you and get really good at it before moving on or trying out something else. So, with all that said, I'm going to show you my tech stack. A lot of people will have different opinions on this, but after working in the industry for many, many years, this is a solid tech stack that's used all over the world by massive companies. A quick shout out to Scrumar, the sponsor of today's video. The problem with most coding courses is that they're passive. You watch someone else code for hours, you feel like you're learning, but then when you sit down to build something, you're stuck. And that is tutorial hell. Scrimbar is completely different, though. The lessons are interactive. This means you can pause the video at any point and edit the instructor's code right there in the player. I've always been an advocate for learning by doing. Script has been very well known for front-end learning, but last summer they began offering support for backend and full stack languages.

**[00:02:39]** Their full stack developer path offers over 100 hours of content designed to take you from zero to job ready in 6 to 12 months. It covers everything you need to know to be a full stack engineer. JavaScript, React, Node, Express, Nex.js, TypeScript, SQL, and they even have AI engineering built in. Everything runs in the browser, too. So there's no messing around with local setups. You just open it and start coding. Scrimar has over 2 million learners, an active Discord channel with over 70,000 members, and they've even partnered with Mozilla MDN on their curriculum. They've also got tons of free content if you just want to dip your toes in first. But if you're ready to go allin, I've got a link in the description with 20% off Scrumar Pro. And if you grab the annual plan, you save even more on top of that. Thanks to Scrimar for sponsoring this video. So the very first component of this tech stack is the front end. The very same thing we're currently looking at. So this is our front end or user interface. And irrespective of what application you're building, there should be some way for the user to interact with your application. This is usually called the UI or the user interface. And the actual navigation through that application is called the UX or the user experience. So what is the kind of feeling that we want to give people when using our application? Now for our little application, we are using a framework called React. And React is incredibly powerful. You might also hear people refer to it as Vit or Vit React, but it really is the same thing. And React has actually been around for a long time. I think it's been around for more than a decade already. And it's used by so many companies out there. It was actually developed by Meta to be used in Facebook, but it's really gained traction and it's used all over the place. Another really cool side effect of learning React is that if you want to build mobile applications like Android or iOS apps, you can use React Native.

**[00:04:40]** It kind of uses the same syntax and the core components of React. So, if you're familiar with how React works and how to prompt your agent to build for React, you can very easily build mobile applications as well using React Native. And finally, if you ever used any AI builders like Bolt or Lovable, these use React for the front-end components as well. Of course, there are many other options as well. React is not the only UI framework out there. Swelt is also really popular, especially in larger organizations. And another really popular framework is Astro. So, you can use this to build anything from marketing sites, blogs, e-commerce websites, and more. And honestly, all of these different frameworks are incredible. But in my opinion, you cannot go wrong with using React. Let's continue working on our app. We will notice that we have a small problem.

**[00:05:28]** Let's just put in something like list and URL to list. When I click on save, our item is added down here. But watch what happens when I refresh this page. When I click on refresh, all of our data disappears. And this data is not being persisted. Data persistence is something that a lot of people struggle with. But there are a few very important things that you do need to understand. If you're building a UI only application like this, then we can actually store the data in the browser's local storage. If we press F12 on the keyboard, we can click on the applications tab and then here we've got this local storage option. This allows us to store data as key value pairs. So this means we could store data in this little data store and our application will be able to retrieve data from that store as well. So we could ask our agent, hey please can you extend our application? I want all of this data to be stored and retrieved from local storage and let's send this.

**[00:06:32]** And while our agent is cooking let's add another section here for data storage or let's call it data basis. And for the first option, we'll have a look at local storage. And our coding agent is done. So let's actually add an item like OpenAI news. I'll paste in a link to OpenAI news. Let's save this. And watch what happens when I refresh this page. Our data is still there. And I can refresh it as many times as I want. And this is because if we have a look at our local storage, we have this bookmarks object which contains the information related to the bookmark that we just added. And of course, if we remove this item, it will be removed from local storage as well. The problem with this type of storage is that this data is only available to this browser session. If I open our website on my mobile device or even on a different browser, I won't see my data. For example, if I copy this URL and I open this in a different browser, I can't see any of that data. So, how do we solve that?

**[00:07:38]** Well, what we have to do is instead of storing the data in the browser, we'll use an actual database. You might have heard of SQL light. This is a tiny little database that gets created within the folder of your project and then all the data will be stored in that file instead. So we could ask our agent, hey I don't think storing it in the browser is the ideal solution. Please can we store the data in a SQL light database instead? Right, let's send this. All right, so just implemented this database. So I'm going to refresh the app. Let's call this open AI news again. And look at that. The item was saved. And if we refresh the browser, the data is still there. And just to make sure we are using the database, we can see that local storage is actually clean. But let's go back to our project for a second. In our project folder, we now have this bookmarks db file. And if I move my camera for a second, we can get a nice view of it. In this little database file, we have a bookmarks table. We can see this entry with ID1 with the title of OpenAI news and the URL that I provided. So instead of us storing the data in the browser, we're now storing it in this local file, this little database instead. This also means if we run this app in another browser, the data is still there as well. Now, this specific scenario is more common than you would imagine. And this is the trap that I see a lot of Vive coders step into. We didn't really give the database any thought. We just said, well, we don't want to use local storage, so just use SQLite because we heard it as a buzzword somewhere. But this actually introduced several issues into our project. So although the project seems to be working right now, trust me, when we try to deploy it, we're going to have a nightmare of a time. And let me try to explain why. In order to get this local database to work, Claude had to create this server file. And the server is a really important component. And let me just try to draw this up. So we have our UI and we have our database which contains all of our data. Now for this I am not referring to the local storage in your browser. I'm referring to an actual persistent database like the SQLite database that we just created. And that database is sitting on our server.

**[00:10:00]** Right? It's living within our source code of our project. Now one very important note is that our UI our front end is not able to talk directly to the database and that is by design. The data that we store in this database could be very sensitive. Right? This could be like user information. It could be passwords. It could be whatever. And we do not want anyone who's got access to our UI to have some sneaky way of getting into the database and retrieving whatever data they want. So therefore, it's definitely not recommended at all to connect your UI, your front end directly to your database. Techsavvy people can simply test your application, look at the network tab, and figure out exactly how they can interact with your database to retrieve data or even maliciously inject data into your database. So in order to implement any sort of guard rails and safeguards, what we have to do is implement a back-end server. So you might hear people refer to this as the back end or they might refer to it as a server. It's really the same thing and it's on this server where our project files actually live. When a user reaches out to our website, the server will actually return the HTML file. So everything that the user sees and when the user submits a form or requests data they will call this server and say hey the user just clicked on this button or the user wants to view a list of data please return that data back to the UI. The server can then implement all sorts of rules and logic to say well are you actually authenticated? Are you allowed to view this data and only then return the data back to the front end. So if the server determines that you are allowed to view this data, it will reach out to the database, the database will return the data back to our server and our code can then actually look at the data, filter out anything that shouldn't be viewable by the user and return the data back to the front end in a clean and structured list. So that is why when you ask your agent to store data, it will sometimes create this weird server file and install a whole bunch of dependencies that just do not make any sense. Now, it's also at this point where we've kind of lost control over what's going on in our application. We started off with a React front end, but what happened now?

**[00:12:22]** So, it's created the server file and the server is using Express and it even installed a whole bunch of dependencies. So, if we go to our package.json file, it installed a package called Express with Course. It installed better SQL. And we also have this new script to start the server itself. So now when we want to run the application, we actually have to start the dev server which is the front end and we also have to start our backend server as well. So both of these need to be started in order for our application to work. This also makes deploying the application to production so much harder because now we have to not worry about just one thing like the front end. We also have to install the backend server and we have to ensure that the UI actually knows where the backend server is living because typically when it requests data from the server it's calling an IP address or like a URL to retrieve the data and this will be different depending on whether you're running the server locally on your machine or in production. So this means that any sensitive processes like reading or writing to the database, checking passwords or even processing payments needs to happen somewhere that the user can't touch. And that is your backend server. Your server is just a computer that's running your code in a secure place that only your app can talk to. It's kind of like a restaurant. The front end is the dining room where the clients would sit and have their meals.

**[00:13:48]** where the back-end server is like the kitchen where all the work really happens and clients should not have access to the kitchen area. Now, implementing this back-end server might seem really scary. As we saw earlier, it could just be a really simple file that the agent could create that securely runs all of this code on the backend server, but there's actually an easier way to do this. We could use something like Nex.js, JS which is a meta framework that handles both the front end as well as all that backend logic. NextJS actually supports React out of the box but it also handles all that code that should securely run on the back end as well. Tools like Lovable and Bolt actually have a different way of approaching this. If you have a look at the code, they do use React as the front end, but then if you need to add any back-end functionality, they actually integrate with something called Superbase. So basically all they do is say well we'll handle the front end but if you need any code that needs to run on a server we'll hand all of that responsibility over to superbase and superbase will deal with the database authentication storage etc. But if you've ever used these platforms you know how difficult this can be managing the front end and the backend separately can really be tricky. Honestly I would suggest starting off with something like Nex.js JS and getting really good at it.

**[00:15:11]** It just really simplifies the code as well. Just to show you, I just migrated our project over to Nex.js and we no longer have a separate server file and the separate folder for the front end either. And if we have a look at the package.json file, this has been simplified greatly. We don't have different scripts for starting the front end and the back end. The front end and the back end just talk to each other. It just works out of the box. So if I want to start this project, I can just run npm startdev and everything still works. And trust me, you can build enterprisegrade software using this framework. Now before we move on to exploring the tech stack, there is a message I really want to get across to all of you. And this is something I discuss with my school community all the time. I know if you're a VI coder, you might be afraid to learn the technology because you really don't want to understand the text stack or the languages itself. But I encourage you to read the documentation. Nex.js has a lot of docs. They've been around for a very long time and the documentation is complete and actually really easy to read. You'll be surprised if you go through the getting started guide and how clearly they actually explain how all of this works. It's definitely worth your time. And if you still feel like the documentation is too complex for you, use your agent to teach you how all of this works. These coding agents are phenomenal teaching tools. So, simply open up something like Claw Code and ask it to explain the tech stack and everything that's going on in layman's terms. Spend a little bit of time just to understand what's going on. It's going to help you understand why everything is there and help you build better software going forward. Our app is already starting to feel like it's moving in the right direction. We're using Nex.js JS as the underlying framework which is already a really solid foundation but we do have a bit of a problem still we're using SQL light.

**[00:17:04]** Now this is something that only developers know who's been working in this industry for a while. If I ask you as a developer to build a proof of concept chances are you're going to use SQLite. It's very common to use SQLite just to build a quick PC with the intent of deleting that PC or migrating it over from SQLite to an actual database once we're ready to hit production. But these coding agents don't tell you that. If you tell it you want to persist data, it's most likely going to use SQLite because it's the convenient thing to do. But as a VI coder, you don't understand yet that this is throwaway and you still have to migrate this to an actual database. This is a huge gap that so many people fall into. Now, I'm going to try and explain the issue here, and it might not seem very obvious. This server, I'm actually just going to move the database out of the way for a second, but this server is used to run our application's code. Now, technically speaking, this diagram wasn't 100% correct. Our database is not living outside of our server. It's actually currently living within the server along with our project's code. Now, in a production environment, this is not going to fly depending on the server that we're deploying to. A very common use case is to deploy these applications to something like Netlefi or Versell.

**[00:18:26]** And those applications do not allow you to run a SQLite database within the project's folder. Those instances are really just meant to execute code and not store anything like databases or dynamic assets like files. This means that any changes to this database like if we added any records to the database will be deleted whenever the server restarts which is definitely not what we want. So instead what we want to do is set up the database outside of our project's code. And this is really easy to set up. Let me show you. For development, we could simply ask the agent to create a Postgress database and then run it in Docker Desktop. So all you have to do is install Docker Desktop and this will simply work. So let's ask our agent to set this up for us. Hey there. So currently we're using SQLite, but this is not an ideal solution. We need to deploy this application to production at some stage. What do you recommend we do? And let's see what Claude came up with. What I'd recommend is a Postgress SQL database hosted via Neon, Superbase, Railway or any managed provider and we'll use Drizzle OM as the query layer. It's lightweight, type safe and pairs well with Nex.js. As you can see now that we told Claude that we plan on actually deploying this to production, it's changed direction completely. It's saying well you can't use SQLite for that. So instead we need to use a database like and then in order for our application to connect to the postgress database we'll use a tiny little library called drizzle OM this is a little bit technical but it's not that hard to understand all right help me migrate the solution over to a postgress database then with drizzle OM in order to run the database locally I do have docker desktop set up so please create a docker compose file for starting up the database. Right, let's send this. And just like that, we have a Postgress database running on our own machine which we can use in our project. You will also notice that the SQLite databases are gone. So, we're no longer using those local files in the project folder, but instead we're connecting the project to this Postgress database via this connection string. Now, this is going to be pointing to a local database on our machine. But once we create a database in neon or superbase or versel, you will just copy that actual production string and use that in the environment variable. So let's start our project again. Let's just add something here. And this item was indeed saved.

**[00:21:04]** And if we refresh the page, the data is persisted. Our app is really starting to take shape. And we now have a much better understanding of what the underlying components of what this app are. We're using React for the UI. We're using Nex.js for both the front end and all the backend work. We're using Postgress for our database and Drizzle OM to connect our backend server to the database. But one thing that's definitely missing here is user authentication. Most apps that we build need some kind of orth system. And if you simply rely on your coding agent to figure out the or system, you're really putting your entire application and user data at risk. This is something you do not want to vibe code or take any chances with. Security is critical. What I suggest is using something like better or it's a really simple library that you install into your Nex.js project and everything just works. It also has integration with pretty much any provider you can imagine. Again, definitely read the documentation to understand how all of this works. But if we just want to speed things up, I'll simply paste in the URL to the installation. And for this demo, I'll just use email and password verification. So, let's paste in that as well. And it's say let's add user authentication to our app. Here are the docs to installing better or as well as using email and password verification.

**[00:22:33]** And now, if we go to the app, we have to sign in with our email and password or we can sign up. So, let's actually do that. And now that we're signed in, we can add our items. And now we will only see the items that belong to us and not to other users. Now, there's obviously a lot more we could discuss in terms of more complicated text stacks. But for 99% of all use cases, this is the best starting point. As I promised, here's a quick tip for just getting started really quickly. When you plan your next project, do not just tell the agent to go and build the application. Add this one single line and you'll see a massive difference. Add something like, "Please help me plan this app that I'm trying to build. This is not a prototype. This is not a P. This is not an MVP either. This application and its tech stack needs to be production ready. Also, we are planning to deploy this application to Versel. What tech stack would you recommend?" And that my friend is really the secret source. Simply tell the agent what your intentions are, like where you're planning to deploy this to and it's going to make a massive difference.

**[00:23:45]** The agent will never propose something like SQL light or an or system or something that's not going to work on Versel or whatever that target environment is. Or finally, to speed things up even further, you can use my free template, this free boiler plate right now. I'll link to this in the description of this video. And all you have to do is simply copy this command. So this guy over here, and if you're starting off with a new project, just open up the terminal, paste in that command. In fact, I'm just going to replace this with a period. So we install this in our current working directory. Then I'll just select PNPM. Or you could just select npm. It doesn't really matter. And look at that. This is going to copy across all the files to set up your docker compose file for your Postgres database. This is a nextJS application with better orth and all the other dependencies that we just discussed all being installed through a single command. I hope you enjoyed this video. If you did, then you might want to consider joining my school community, Agentic Labs. This is a vibrant community of people who build AI solutions and to learn and share ideas with each other. We also have weekly challenges where you can test your skills as a vibe coder or a gentic coder. And of course, we also have classrooms on all the popular tools like Claude Code. And we also have live Q&A sessions every Wednesday. And the best part is it's only $7 per month, which is an absolute steal. And YouTube thinks you'll enjoy this other video right here. So I'll see you in the next one.

**[00:25:21]** Bye-bye.
