---
title: "Claude Code Skills Now Build AND Test Themselves"
video_id: epZy_NajGnA
date: 2026-03-24
url: https://www.youtube.com/watch?v=epZy_NajGnA
channel: Leon van Zyl
duration: "20:38"
source: auto-generated
---

# Claude Code Skills Now Build AND Test Themselves

**[00:00:00]** If you're using agent skills the same way you did 6 months ago, you're missing out on a lot. Skills can now fork off into their own sessions and inject live project data even before the agent sees the prompt. And now there's a built-in system that not only creates the skill for you, but it also run tests and it will run a blind AB comparison to prove that everything works. Today we're looking at all of the new features added to agent skills. So let's get into it. Now, before we dive into the advanced stuff, let's start with the basics. Installing skills. Now, there's actually two ways to go about this. Although we're using Claude Code in this video, Claude Code is not the only CLI tool out there. There are a lot of other genting tools out there like Cursor and Open Code and Codeex and pretty much all of these support skills. So, if you're just focusing on CL code, the easiest way to go about this is to run the plug-in command. And in here you can simply search for all the available plugins and we can just search for the skill creator skill. Now if you don't see this in your list, what you need to do is simply tab to the right to marketplaces. And you have to make sure that you've got this cloth plugins official marketplace installed. It should be there by default, but if it's not, I'll leave a link to this GitHub repository in the description. So you can simply copy this repo. So all you have to do is go to add marketplace, paste in that URL, and then you should have access to all of these different skills. Now the one that we are interested in is the skills creator skill and we will get back to it in a second, but you can just go ahead and install this skill into your project.

**[00:01:40]** Now if you're not using claw code, so maybe you're using open code or cursor or whatever else, there's actually a really cool way to find skills as well. Simply go to skills.sh. From here, you can actually search and browse all sorts of different skills. Then search for the skill creator skill. Then let's select the one from anthropic specifically. Then simply copy this command. And then back in our project, we can simply run this command. And check this out. We now have access to pretty much every possible coding tool you can imagine. So we can press spacebar to select or deselect any tool like claw code. And maybe we're also using something like I don't know like kilo code or whatever. I'll simply select claw code and press enter. I'll then install it at project level and then I'll select sim link and proceed with installation. This will download and install the skill in two different folders. We've got the claude folder of course for claude code and we also have the agents folder which is generally used by pretty much all of these other agentic coding tools like cursor or codeex as an example. So if you do want these skills to be available to all of your other coding tools then this is really the workflow you should be following. So let's have a look at the skill creator skill. Why do we actually need this? Well truth be told you can manually create skills yourself.

**[00:03:05]** It's actually straightforward. You just open up the claude folder or the agents folder. Then within the skills subfolder, we can create a new folder and let's just call it something like review code. Then within this folder, we can create a new file. And we just have to call this one skill.md. And then we just have to start the file off with these three dashes. And we have to end it with three dashes as well. This is called the front matter of this file. And this needs two values, the name and a description. So for the name, we can just call it something like review code. And for the description, we can enter something like use this to review code. This is actually really important as this will tell the agent when to use the skill. And then honestly, we can provide any prompt we want. So what will happen is when we ask agent to review code, it's going to pull in the skill and then follow these instructions step by step. Now, of course, we're not here to build custom skills. We can use the skill creator skill to automate the process of building skills for us. The skill creator skill includes all of the documentation and examples and everything the agent needs to create detailed and powerful skills for us.

**[00:04:20]** What I do want to point out is that Claude made a lot of improvements to the skill creator skill. So of course it will still create the skills for you but now we can also use it to test the skills and it can do evaluations and AB testing on those skills as well but anthropic defines two types of skills capability uplift and encoded preferences. With capability uplift we can get the model to provide the responses better than what the base model would be able to provide or we can add a new capability to the model as well. A practical example for cloth code is the ability to generate images. This is something cloth can't do natively, even using their own app. But when doing web design, sometimes we want to generate our very own stock images and we can use something like nano banana or some kind of image generation model to generate those images for us. Or another example of capability uplift would be something like front-end design. By default, the models tend to generate these AI sloppy kind of designs. But if we give it a very specific front-end design skill, we can greatly improve the results. With encoded preferences, you basically define a workflow that the agent needs to go through. So these are individual capabilities that the agent already has like the ability to go online and retrieve documentation or you know writing results. It can already do those things. But maybe you've got a workflow that requires a very specific sequence of events. So with encoded preferences, you can create a skill to define that exact workflow. Other changes include the ability to create evaluations, run tests, and even do blind AB testing to see what the difference would actually be. Now, we will have a look at this in this video, but this is actually a really important feature. As these models get progressively better, chances are that something that could be a limitation now. So let's say you've created a specific skill to get a very specific result. But for argument sake, let's say Opus 5 comes out and now maybe Opus 5 is able to natively perform that task without your skill. So you can constantly evaluate your skills against these new models to see if they're actually still relevant or maybe your skill is performing worse than the native capabilities of the agents or the harnesses they work in as well. Now enough about the theory. Let's build our very first skill. Using the skill creator skill is really simple. So all we have to do is pass a prompt describing the skill that we're trying to create. Like use the skill creator to create a new skill called readme gen. It should generate professional readme.md files for any project by analyzing the codebase structure dependencies and git history. All right, cool. So the agent created the skill. Let's go to our claude folder. We now have this readme gen skill. And just like I showed you previously when doing it manually, we have this skill.mmarkdown file with this front matter with a name and a description. So everything we've seen already. Then we have all of these different instructions that the agent can follow to create a readme file that matches our preferences. So of course if we want to we can edit this file however we want or we can just ask the agent to make any adjustments for us. And invoking the skill is really simple.

**[00:07:42]** Either we can just enter front slash and select a skill like a custom command basically. And that is a really important point. skills and custom commands have actually merged. And at this point, there's really no point in creating custom commands anymore. Just create a skill and you can call it like a custom command. We can also see all the skills that are available by running the skills command. And here we can see the readme gen skill. Now, of course, we don't want to run the skill manually. We'll typically get the agent to do it for us. So, we could say, "Hey, please update the readme file." We could specify that it needs to use this specific skill, but as you just saw, the agent is usually intelligent enough to realize that it's already got access to that skill. So, it's going to pull it in. So, we can see this little section over here. Now, let's start diving into the more advanced features and really cool workflows of working with these skills. So, the reaping gen file didn't take that long. It was only like a minute or so, but certain skills can take a hell of a long time to complete.

**[00:08:42]** Honestly, I've got a skill that actually reviews pull requests and for each pull request, it will dive into the code to see if the change is safe to merge and then finally give me a detailed output of which PRs are ready to go and which ones are high risk. That can actually take a really long time to complete, like 20 minutes sometime. So, here's a quick tip for improving performance when it comes to using skills. This is going to deviate slightly from skills, but I do think you're going to appreciate this. Back in Claude code, I'm going to run the agents command. Then let's create a new sub agent at project level and we'll generate it with claude. So what we can do is actually set up sub agents that will use our skill, but this will actually run in the background automatically. So we can provide a prompt like your role is to update the readme.md using the readme gen skill. And here's the important part. Set background to true for this agent. What background true will do is automatically run this agent in the background which will free up our main agent to do other work. So let's just choose a color and this is add project scope and cool. If we go to claude I'm just going to have a look at this agent file for a second and it seems like claude didn't add it automatically. So in this file I'll just add background to true. So instead of calling the skill directly, what we can do is simply say use the readme gen agent to update the readme file. And that's really it. This readme gen agent is now running and it's automatically running in the background. And we can continue with the main agent. If we press down, we can actually press enter on this local agent. And now we can see exactly what that agent is up to. So this can be really helpful if you've got longunning skills. Now, here's another tip for dealing with skills. Some skills are massive. When the agent pulls that skill into context, it could load all sorts of scripts and reference files.

**[00:10:40]** For example, let's grab this Versel React best practices skill. I'm just going to copy this. Then, let's install it into our project. And you'll see exactly what I mean in a second. This skill obviously contains the main skills. ND file, but this file is nothing more than references to very detailed documentation on working with Nex.js and React. So, it's pointing to all of these different documents which we can find in this rules folder. And in the rules folder, you can see there are a lot of reference docs here. And the problem is when the agent uses the skill, all of this context sitting in his files are going to be pulled into the context window, the main context window. So this can very easily muddy the conversation. So thankfully there's a new capability in skills that we can use to run the skill in its own dedicated session. That means the skill will run in its own session with its own context window and then only the summary or the final result will be passed back to the main agent. All we have to do is in the skills markdown file add a new property called context with a value of fork. In fact before we run this let me just show you the difference. So let's just spin up CL code. So without the fork context, let's run read gen. And this is very straightforward. The agent will pull in the details with regards to that skill. And then all the processing is happening in this main thread like reading all the different files in. But if we add context fork, this is going to look slightly different. So again, I'll just run gen. Of course, if the agent invokes the skill, it's the same behavior. What you'll see now is that this readney gen skill is running within its own thread. Now the main agent is simply waiting for the result. Now I do want to mention this is synchronous so you can't exactly continue the conversation with the main agent but it does free up the context window which is a big deal when you're working on a really complex task. There are a ton of skills out there for claude code and one that caught my attention recently is from today's sponsor airtop. They're a platform that you build AI agents that run tasks in real cloud browsers. And their skill gives your coding agent the ability to use that whenever it needs to. You install it with one command and now when your agent needs to interact with any website, it can spin up a real cloud browser through airtop. Navigate the site, handle any anti-bot protections, and bring back your data.

**[00:13:11]** You just describe what you want in plain English and your agent figures out when to use airtop to get it done. So, say you need data from a site that doesn't have an API or you want to monitor a page for changes. Instead of trying to hack together a scraper that's going to break the moment something changes, you can just ask your agent to handle it. It handles the airtop skill to do the browser work and you get the result back without having to think about what happened. It's just another capability your agent now has. You don't have to learn a new platform or switch context. Your agent just knows how to use it when it needs to. You can try airtop yourself using the link in the description below. If you use code Leon, you will get a free month of their starter plan. Thanks to airtop for sponsoring this video.

**[00:13:54]** Now, let's look at something really cool. This is going to seem a little bit technical, but once you get it, it's super super powerful. It's called dynamic context injection. So, the best way to think about this, it's kind of like a pre-processing step. So in this actual skill set file, we're going to add a placeholder for something. And when Claude needs the skill, this script is going to run first to populate those placeholders with actual values and then Claude will have a look at it. To give you a super simple example of what this could look like is we could have a section called project context. And now we just want to give the agent the name of the project. But of course, this is a dynamic value. It will always depend on the project that you're currently working in. So what you could do is rely on the agent to look at the file structure and figure this data out itself. But it's going to take time and waste tokens. So instead you can use a script to preetch and preload all of this data into this file from the get- go. So to inject any dynamic context you start with this exclamation point. Then in back ticks we can pass in any shell command. So this will depend on your operating system but on Windows it's just pwd for project working directory.

**[00:15:14]** In fact if I took that command and I just ran it in powershell this will return the full path to the project. So that means when the agent needs the skill it's first going to run this pre-processing to dynamically inject this value into the prompt. But of course this is not really a practical example. It won't take the agent very long to get the project name. But what if the agent needs the entire project structure and it needs to kind of dynamically find a whole bunch of different files? Yes, it could fire off explore agents, which we've all seen before, but it takes a lot of time and it burns through context. Remember, tokens cost money. So, being able to dynamically inject all of that context into this skill before the agent even reads it is really powerful. And of course, we don't have to populate this ourselves, by the way. We can simply pull up CL code and we can provide a prompt like use a skill creator to improve the readney chain skill. Add preprocessing commands that inject the project file tree, the contents of package.json if it exists, and the last 10 get commits. So, another way you can go about this is to open up claw code, pull in a skill that you already have.

**[00:16:28]** Maybe you've got a skill that actually requires a lot of project context and simply ask Lord to use the skill creator to improve that skill by potentially adding pre-processing commands. It will know exactly what you mean and add all of it automatically. Anyway, let's run this one. Right, Claude is done. So, let's have a look at this project now. Of course, we still have our little silly example over here, but this is really where the power of dynamic context injection kicks in for the file tree. And by the way, that's like all of these different folders and subfolders and files. That's all now being handled by the script. So this will tell the skills file to dynamically execute this command whenever the skill is needed. And this simple little script will return everything related to our project structure. Now just imagine the amount of time and tokens that you've typically burned using claw code where they had to explore the project to get this information. and it's probably done that every single time you ask it to update the readme file. Now, this is completely free. And just to show you what that would look like in the terminal, when I run it, it actually returns the entire project structure. So, all of that context will automatically be injected into this prompt. The same thing goes for the contents of a file, like the package.json file. And of course, we're also pulling in the git history as well.

**[00:17:50]** So, anything that you could run in the terminal, just add it to this file. And finally, this is one of the coolest features in the new skill creator skill. The new version of the skill creator allows us to create evaluations and actually test the skill and even run AB comparisons between using the agents with and without the skills. And all we have to do is say use the skill creator to create evals for the readme gen skill. Now, what Claude will do is create this eval subfolder. And these will basically be a whole bunch of testing scripts that I can use to run evaluations against our skill. And of course, like with all of this, we can jump in and change that file oursself. But I'm not going to change anything. Instead, I'm going to ask Claude to run those evals. Please run the readme gen.

**[00:18:41]** This is again pulling in skill creator. Now, it's reading the evals and setting up the workspace. Let's set up the workspace and launch all six rungs. What's really interesting about this is that cord is actually creating work trees. So each eval actually has a separate folder in which the testing is taking place. And now that those folders have been created, it's running six agents in parallel to perform these eval tests. And we do get a bit of a summary on what each eval is about. And for each evaluation, the agent is testing it with and without the skill. So baseline just means without the skill. So, while these evals are running, I'm just going to do a quick shameless plug. If you want to take your Aentic skills to the next level, then consider joining my school community, Aentic Labs. To be honest, that's how I keep up to date with everything related to Claude Code. And we also have a structured classroom on Claude Code as well. And it's only $7 per month for the first 1,000 members.

**[00:19:38]** So, honestly, it's a steal. So, let's just have a look at what Claude is doing. And it seems like it's possessed. I just find that the claude CLI is just so broken sometimes. And finally, Claude actually opens up this eval viewer which gives us some output, but the main thing really is this benchmark tab where we can see how our skill stacks up to the standard behavior of the agent. So, we've got all sorts of different metrics. And here we can see with and without the skill. And this can be extremely useful for gauging the performance of running all of this with and without the skill. And with something like the assertions, we can see if Claude would have done all of this by default and if the skill actually made any difference at all. Of course, we've got a very simple skill, but you definitely want to run this against your very own skills as well.

**[00:20:29]** So, I hope you enjoyed this video. If you did, hit the like button and subscribe to my channel for more Claude Code content. I'll see you in the next one.
