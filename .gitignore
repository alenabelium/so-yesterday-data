# Raw subtitle files (json3) — regenerated by the pipeline; the cleaned-up
# transcripts in channels/<id>/transcripts/ are tracked instead.
channels/*/data/raw/

# OS junk
.DS_Store

# Python caches (shouldn't appear, but guard against accidental writes)
__pycache__/
*.pyc

# Editor swap / backup
*.swp
*~
